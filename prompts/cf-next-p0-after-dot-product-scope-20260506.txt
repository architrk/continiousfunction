You are helping Continuous Function choose the next concrete product slice.

Project:

- Continuous Function is a Next.js Pages Router + TypeScript learning atlas for the mathematics behind modern deep learning and frontier AI.
- The product goal is slow-growth, high-quality, mechanism-first learning: intuition, math, runnable code, interactive demos, source grounding, and object-attached research prompts.
- The site must stay useful as a static export. For this slice, do not introduce auth, database, API routes, protected pages, dynamic atlas reads, external accounts, or broad platform work.
- We use Oracle/GPT Pro as a scoping and review partner. I will implement one narrow slice, then send it back for publish/no-publish review.

Current branch:

- `private/research-learning-loop-20260505`

Recent P0 prediction-first in-demo retrofits already shipped and pushed:

- Reverse-Mode Autodiff: cotangent accumulation reveal.
- Probability Basics: conditioning/posterior movement reveal.
- Random Variables: pushforward/fiber reveal.
- Variational Autoencoders: ELBO gap diagnostic reveal.
- Tree Search Reasoning: visible max-backup root-branch reveal.
- Dot Product: signed scalar projection reveal.

The current audit still says the full north-star goal is not achieved. The active P0 remains:

> For live demos that predate the demo-shell rollout, add explicit concept-specific "predict before reveal" states and compact demo-state emission. A global demo-shell checkpoint now gives every published live demo a baseline prediction prompt, so the remaining work should focus on deeper in-demo reveal states for critical prerequisites and flagship-adjacent pages.

Current local scan after the Dot Product commit:

1. `linear-algebra/vector-spaces`
   - Co-located `viz.tsx`.
   - Critical prerequisite.
   - Currently emits demo state immediately for u, v, a, b, au, bv, w, determinant, parallelogram area, span status, |w|, and parallelogram visibility.
   - UI immediately exposes `w = a u + b v`, determinant, area, and span status.
   - Likely local reveal candidate: learner predicts whether draggable u/v span a plane, nearly collapse, or collapse to a line before determinant/area/span status are revealed.
   - It should not become a full basis/linear independence lesson; it should teach closure plus span collapse as a vector-space mechanism.

2. `calculus/derivatives`
   - Co-located `viz.tsx`.
   - Critical prerequisite.
   - Currently emits demo state immediately for function, x, x+h, h, f(x), f(x+h), secant slope, tangent slope, slope gap, convergence status, and visible layers.
   - UI immediately exposes tangent slope and slope gap while the learner adjusts h.
   - Likely local reveal candidate: learner predicts whether the secant slope rises, falls, or stays near the tangent as h shrinks, or predicts derivative sign/magnitude class before tangent/gap are revealed.

3. `calculus/computation-graphs`
   - Co-located `viz.tsx`.
   - Critical prerequisite.
   - Currently emits demo state immediately for forward/backward values and sensitivities.
   - In Backward mode it immediately exposes direct/via-sine contributions, accumulated bar a, bar x, and bar y.
   - Potential issue: Reverse-Mode Autodiff already got a similar cotangent-accumulation reveal, so choose this only if it can teach a distinct computation-graph mechanism rather than duplicating that slice.

Other published concepts already have some concept-specific prediction checkpoint or route wrapper, but the P0 is still not globally closed. If you think one of the three above is not the right next target, you may name a better single target only if it is clearly higher leverage and still bounded.

Decision needed:

Choose the next concrete P0 slice after Dot Product. Do not design a broad roadmap. Pick one route/component and define a narrow, publishable implementation scope that improves joy/depth/rigor without introducing platform work.

Please return:

- The single best next slice and why it beats the other candidates now.
- Exact file(s) to edit.
- What should be visible before reveal.
- What must stay hidden before reveal, including any demo-state leakage.
- The learner prediction choices.
- What measured demo state should be emitted after reveal.
- Required stale-state clearing behavior.
- Minimal tests and browser checks.
- What is explicitly out of scope.
- Any likely pre-reveal leak in the attached files.

Important constraints:

- Do not close the overall P0 unless this single slice truly completes it; assume it does not.
- Prefer a bounded mechanism-first slice over broad platform work.
- Prefer concept-specific prediction gates and compact measured-state handoff.
- Avoid duplicating recently completed routes unless the mechanism is clearly distinct.
- Keep final implementation static-export-safe.

Project: Continuous Function, a Next.js Pages Router + TypeScript learning atlas for modern deep-learning math.

Current state:

- Repo branch: `private/research-learning-loop-20260505`.
- Product goal: make the site deep, joyful, rigorous, interactive, and worth returning to, while growing slowly through review-gated concept slices.
- We are continuing the P0 prediction-first demo retrofit pass.
- Recently completed in-demo P0 slices include Reverse-Mode Autodiff, Probability Basics, Random Variables, Variational Autoencoders, Tree Search Reasoning, Dot Product, Vector Spaces, Derivatives, and Computation Graphs.
- Latest shipped slice: `calculus/computation-graphs`, a reused-node sensitivity reveal. Oracle first returned no-publish due to static MDX answer leakage; after rewriting the Math/Code sections and adding pre-demo leak guards, the fix review returned publish and the private Vercel preview deployed successfully.

Constraints:

- Keep the next slice mechanism-first and bounded.
- Prefer one concept route or visualizer.
- Do not introduce auth, database, API routes, protected pages, dynamic atlas reads, broad platform work, or large curriculum rewrites.
- Preserve static-export safety and the existing shared `emitDemoState` checkpoint.
- The result should hide answer-bearing backward/update mechanics until the learner makes a concrete prediction, then reveal compact measured state for the AI companion handoff.
- The implementation must be practical for one Codex pass with tests and browser checks.

Candidate context:

- Strong candidate: `calculus/backpropagation`. It is the next critical prerequisite after Reverse-Mode Autodiff and Computation Graphs, and the audit says it currently emits two-layer learning-signal state and visible update readouts, but it does not yet have the same local prediction-before-reveal answer-hiding rigor as the newly completed slices.
- Comparison candidates attached: Gradient Descent, Maximum Likelihood, Cross-Entropy, Distributions, Bayesian Inference, and recent Computation Graphs. These appear to already have concept-specific prediction checkpoints and are attached so you can avoid repeating already-good-enough work.

Question:

Pick the next best P0 concept-specific retrofit after Computation Graphs.

Return:

1. The single recommended next concept/route.
2. The exact prediction question learners should answer.
3. What must remain visible before reveal.
4. What must be hidden before reveal.
5. The classifier or expected-answer logic.
6. The compact measured demo state to emit after reveal.
7. The stale-state clearing rules.
8. The smallest tests and browser checks required.
9. Any no-go warnings that would make this slice too broad.

Keep it practical and specific enough that Codex can implement the next slice without asking another scoping question.

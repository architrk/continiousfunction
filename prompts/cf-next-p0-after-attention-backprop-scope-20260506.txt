Project: Continuous Function, a Next.js Pages Router + TypeScript static-learning atlas for modern deep learning math. The product north star is durable, joyful, mechanistic understanding: intuition, math, code, and prediction-first interactive demos that carry compact measured state into the learning checkpoint and AI handoff.

Thread objective: keep growing the site into a deep, rigorous, non-trivial learning environment by doing slow high-quality improvements, not broad idea dumps. Use this review to pick one bounded implementation slice, not a roadmap.

Current P0 audit gap: older demos that predate the demo-shell rollout should get explicit concept-specific "predict before reveal" states and compact demo-state emission. The global demo-shell exists; the remaining work is deeper in-demo reveal states for critical prerequisites and flagship-adjacent pages.

Already completed in this P0 pass:
- Reverse-Mode Autodiff: cotangent accumulation reveal.
- Probability Basics: conditioning reveal.
- Random Variables: pushforward/fiber reveal.
- VAEs: ELBO gap reveal.
- Tree Search Reasoning: max-backup reveal.
- Dot Product: signed projection reveal.
- Vector Spaces: span-collapse reveal.
- Derivatives: secant-to-hidden-tangent reveal.
- Computation Graphs: reused-node sensitivity reveal.
- Backpropagation: hidden learning-signal reveal.
- Scaled Dot-Product Attention Geometry: top-key attention reveal.
- Self-Attention Mechanics: alpha_ij * V_j value-contribution reveal.
- Attention Backpropagation: key-scale gradient-credit crossover reveal.

Recent successful pattern:
1. Keep pre-reveal visible state rich enough for reasoning.
2. Hide answer-bearing values, labels, visual intensity, explanatory insights, and demo-state emission until reveal.
3. Ask one precise prediction.
4. Reveal the mechanism and emit compact measured state.
5. Clear stale local/shared state when controls, tabs, predictions, conceptId, reset, or unmount change.
6. Add focused regression tests and browser checks.

Candidate now:
`components/foundations/TransformerArchitectureViz.tsx` inside the core Attention/Transformers route's "Transformer Block" tab. It currently has architecture variant buttons, a broad gamified architecture-identification challenge, token-flow animation, detail panel, shapes, hover formulas, and immediate `emitDemoState` for variant, focused object, active block, animation state, formula/model sizes, token stage, progress, shape visibility, and quiz phase. The parent route already gates the child tab with a generic "Block composition" prediction, but once the child mounts it is not locally prediction-first.

Question:
Choose the next single P0 slice after Attention Backprop. If `TransformerArchitectureViz` is the right target, specify the smallest publishable concept-specific prediction-first retrofit. If it is not the right target, name a better single file and explain why.

Return:
1. chosen file/component and why it is higher leverage now;
2. exact slice name;
3. the one learner prediction prompt;
4. visible pre-reveal state;
5. values/copy/visual signals that must be hidden until reveal;
6. compact demo-state fields to emit after reveal;
7. test cases that would catch the likely stale-state or leakage bugs;
8. non-goals so the implementation stays bounded.

Please be strict: do not design a full architecture course, do not add auth/database/API routes, and do not require a broad redesign. Pick one small mechanism that can be reviewed, tested, browser-checked, and shipped.

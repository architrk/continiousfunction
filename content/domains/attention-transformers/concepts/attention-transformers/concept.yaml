id: attention-transformers
title: "Scaled Dot-Product Attention & Transformer Layers"
domain: attention-transformers
slug: attention-transformers
difficulty: 3
status: published
importance: critical
prerequisites:
  - maximum-likelihood
  - dot-product
leads_to:
  - rope
  - efficient-attention
  - swiglu
  - induction-heads
  - scaling-laws
related:
  - tokenization-vocabulary
  - decoding-sampling
tags:
  - attention-transformers
  - transformers
  - attention
  - self-attention
has_visualization: true
has_interactive_demo: true
has_code_example: true
math_level: undergraduate
sources:
  - id: vaswani-2017-attention
    kind: paper
    title: "Attention Is All You Need"
    authors: "Vaswani et al."
    year: 2017
    url: "https://arxiv.org/abs/1706.03762"
    note: "Grounds scaled dot-product attention, multi-head attention, and the transformer-layer framing used throughout this route."
claim_checks:
  - id: scaled-dot-product-attention-value-mixing
    status: source-checked
    claim: "Scaled dot-product attention forms queries, keys, and values, uses scaled query-key dot products to produce row-wise attention weights, and mixes value vectors with those weights."
    source_ids:
      - vaswani-2017-attention
    support: "Vaswani et al. introduce the Transformer architecture based on attention mechanisms and define scaled dot-product attention plus multi-head attention as the layer operation that replaces recurrence and convolution."
    caveat: "This checks the core attention mechanism, not later architectural variants, kernel optimizations, or interpretability claims about what any particular head learns."
    evidence_review:
      state: substantive-reviewed
      reviewed_at: "2026-05-06"
      reviewer: "codex+oracle"
      summary: "Checked Vaswani et al. sections 3.2, 3.2.1, and 3.2.2 against the exact claim: the paper defines attention as query/key compatibility weights over values, defines scaled dot-product attention with query-key dot products divided by sqrt(d_k), softmax weights, and value mixing, and describes learned Q/K/V projections for multi-head attention. The row-wise wording is checked through the packed-query matrix formula plus this page's math/code witnesses."
    object_refs:
      - "#source-span-vaswani-2017-attention"
      - "#math-object-1"
      - "#math-object-2"
      - "#code-witness-1"
      - "#interactive-demo"
short_description: "The core transformer operation: compute attention weights from query-key dot products, then mix values to copy information across a sequence."
author: codex
created: "2026-02-16"
updated: "2026-02-16"
estimated_read_time: 18

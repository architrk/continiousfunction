Foundation #19: Efficient Attention at Scale (KV Cache, GQA, FlashAttention)

[Full Oracle response saved for reference - see Oracle session: you-are-helping-build-continuous]

## Key Points for Implementation

**Concept ID:** efficient-attention
**Category:** efficiency
**Icon:** ⚡ (or ⚙️ for systems)
**Number:** 19

**Canonical Papers:**
1. FlashAttention: Fast and Memory-Efficient Exact Attention with IO-Awareness (Dao et al., NeurIPS 2022)
2. GQA: Training Generalized Multi-Query Transformer Models (Ainslie et al., EMNLP 2023)
3. SnapKV: LLM Knows What You Are Looking for (Li et al., NeurIPS 2024)

**Core Math:**
- Autoregressive attention with KV cache: o_t = softmax(q_t K_{1:t}^T / √d_k) V_{1:t}
- GQA grouping: o_h = softmax(Q_h K_{g(h)}^T / √d_k) V_{g(h)}
- KV cache memory: Mem_KV ∝ T · H_kv · d_head

**Visualizations to Build:**
1. KV Cache Budget Dashboard (PRIORITY)
   - Sliders: context length, layers, heads, dtype
   - Show memory scaling with MHA vs GQA vs MQA
2. Decoding Animation (shows growing K/V cache)
3. GQA Grouping Explorer (drag-and-drop query/KV head mapping)
4. FlashAttention Streaming Softmax Explainer
5. Long-Context Compression Playground

**Prerequisites:**
- attention-transformers (#2)
- rope (#18)
- efficiency (#16)

**Enables (Future Concepts):**
- Speculative decoding
- PagedAttention / vLLM
- KV cache compression techniques
- Inference-aware scaling laws
